import { UseBrickConf } from "@next-core/brick-types";

export interface ControlBrickConfig {
  /**
   * 作为表单控件展示的构件配置
   * @description.en Configuration of the brick displayed as a form control
   */
  useBrick: UseBrickConf;
}
